# MultiTeacherCodex browser extension

Load this directory as an unpacked extension in Edge or Chrome.

- Edge: `edge://extensions`
- Chrome: `chrome://extensions`

Enable Developer mode, choose **Load unpacked**, and select this `extension` directory.
Keep the local server running with `npx -y .`.
